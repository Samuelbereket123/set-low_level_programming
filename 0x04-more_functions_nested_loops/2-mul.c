#include "main.h"
#include <stdio.h>

/**
 * mul -checks for two multiplied integers
 * @a: first integer
 * @b: second integer
 *
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
